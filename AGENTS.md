# AI 协作与开发规范入口

本项目的详细规范文档（架构 / 工作流 / 发布 / 测试 / 接手记录等）为内部协作资料，**不在本仓库**。

需要时向项目维护者索取 `docs/project/` 与 `docs/Handed-docs.md`。

## 快速开始

```bash
pnpm install        # 安装依赖
pnpm tauri dev      # 开发调试
pnpm test           # 前端测试
pnpm build          # 构建前端
```

## 许可

见 [LICENSE](LICENSE)。
